function myFunction() {
    var sentence = "I am learning how to connect"; //Using += allows to connect multiple sentences together.
    sentence += " two sentences together";
    sentence += " by using Concatenates!";
    document.getElementById("Concatenate").innerHTML = sentence;
}